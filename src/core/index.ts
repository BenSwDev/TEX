/**
 * Core layer placeholder export to keep the structure discoverable during the initial setup.
 * Replace once real shared infrastructure code is introduced.
 */
export const CORE_LAYER_INITIALIZED = true;

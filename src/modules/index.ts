/**
 * Modules layer placeholder export until real feature modules are implemented.
 * Each feature module should re-export its public API from this entry point.
 */
export const MODULES_LAYER_INITIALIZED = true;
